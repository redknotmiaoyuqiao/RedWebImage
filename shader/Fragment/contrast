precision lowp float;
varying vec2 varyTexPos;
uniform sampler2D mainTexture;

float uT = 1.5;  

void main(){
    vec3 rgb = texture2D(mainTexture, varyTexPos).rgb;

    vec3 target = vec3(0.0,0.0,0.0);  

    gl_FragColor = vec4(mix(target,rgb,uT),1.0); 
}